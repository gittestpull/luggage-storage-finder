# 개발용 Dockerfile - 핫 리로드 지원
FROM node:20-alpine

WORKDIR /app

# 패키지 설치
COPY package.json package-lock.json ./
RUN npm ci

# 소스 코드 복사 (개발 시에는 볼륨 마운트로 덮어씀)
COPY . .

ENV NODE_ENV=development
ENV NEXT_TELEMETRY_DISABLED=1

EXPOSE 3000

# 개발 서버 실행 (핫 리로드 활성화)
CMD ["npm", "run", "dev"]
